#include<bits/stdc++.h>
using namespace std;
int main()
{
	vector<int> v;
	vector<int>::iterator it;
	int n,a;
	scanf("%d",&n);
	for(int i=0; i<n; ++i)
	{
		scanf("%d",&a);
		v.push_back(a);
	}
	sort(v.begin(),v.end());
	for(it=v.begin(); it<v.end(); ++it)
		printf("%d\n",*it);
	
}

